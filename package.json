{
  "name": "trilhas-inova3",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "node api/index.js",
    "build": "cd client && npm install --include=dev && npm run build",
    "start": "node api/index.js",
    "seed": "node -e \"require('./src/db').applySchemaAndSeeds().then(() => { console.log('Pronto!'); process.exit(0); }).catch(e => { console.error(e); process.exit(1); })\""
  },
  "dependencies": {
    "@libsql/client": "^0.14.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.0",
    "jsonwebtoken": "^9.0.2",
    "pdf-lib": "^1.17.1",
    "uploadthing": "^7.7.4"
  }
}
